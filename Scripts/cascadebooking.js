$(function () {
    //$(".pollVote").click(function () {
    //    var $btn = $(this);
    //    var $form = $btn.parents("form:first")
    //    var $container = $btn.parents(".poll-container:first")
    //    var $checked = $(":checked", $form);
    //    if (!$checked.length) return;

    //    var answers = [];
    //    $checked.each(function (i, e) { answers.push(e.value); });

    //    $btn.attr("disabled", "true");
    //    $container.fadeOut(function () {
    //        $.ajax({
    //            type: 'POST',
    //            url: 'Poll/Vote',
    //            data: $form.serialize(),
    //            success: function (data) {
    //                $(data).insertBefore($container).fadeIn();
    //            }
    //        });
    //    });
    //});
});